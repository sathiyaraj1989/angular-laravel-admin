import { ActiveUserFilterPipe } from './active-user-filter.pipe';

describe('ActiveUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
